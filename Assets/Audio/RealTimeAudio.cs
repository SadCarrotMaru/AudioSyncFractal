using UnityEngine;

public class RealTimeAudio : MonoBehaviour
{

    // ma ocup de el in vacanta asta in mod ideal
    // trebuie sa vad exact cum iau sunetul direct, daca e nevoie de ceva 3rd party sa dau redirect de la output la input sau daca pot sa iau eu direct de la windows daca au ceva
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
